name: hello. 

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'hello I am completely new and want to learn'' first issue'
        pr-message: 'pls go slow.as I have no prior education '' first pr'
